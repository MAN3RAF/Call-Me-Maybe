* ~~ handle allowed funcs.~~

2. ~~ **handle get_funcs from input.**~~
3. ~~ **parse the input.**~~
4. [ ] **handle the float() if int -> int
    if number -> float.**
5. [ ] **handle the output to JSON file.**
6. [ ] **GG!!!**
